---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: open-webui-pvc
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: longhorn-basic
  resources:
    requests:
      storage: 25Gi
